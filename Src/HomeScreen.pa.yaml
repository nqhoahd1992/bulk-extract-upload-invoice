# ************************************************************************************************
# Warning: YAML source code for Canvas Apps should only be used to review changes made within Power Apps Studio and for minor edits (Preview).
# Use the maker portal to create and edit your Power Apps.
#
# The schema file for Canvas Apps is available at https://go.microsoft.com/fwlink/?linkid=2304907
#
# For more information, visit https://go.microsoft.com/fwlink/?linkid=2292623
# ************************************************************************************************
Screens:
  HomeScreen:
    Properties:
      LoadingSpinnerColor: =RGBA(56, 96, 178, 1)
      OnVisible: |-
        =Set(gSelectedBatch, Blank());
        Set(gSelectedInvoiceRow, Blank())
    Children:
      - rectHeader:
          Control: Rectangle@2.3.0
          Properties:
            Fill: =RGBA(83, 74, 183, 1)
            Height: =60
            Width: =Parent.Width
      - lblScreenTitle:
          Control: Label@2.5.1
          Properties:
            Color: =RGBA(255, 255, 255, 1)
            FontWeight: =FontWeight.Bold
            Height: =60
            Size: =14
            Text: ="Invoice Batch Upload"
            Width: =500
            X: =20
      - btnNewBatch:
          Control: Button@0.0.45
          Properties:
            BasePaletteColor: =RGBA(255, 255, 255, 1)
            Height: =36
            OnSelect: |-
              =With(
                  {
                      wBatch: Patch(
                          BEUI_InvoiceBatch_Requests,
                          Defaults(BEUI_InvoiceBatch_Requests),
                          {
                              Title: "Batch - " & Text(Now(), "yyyy-mm-dd hh:mm") & " - " & gCurrentEmployee.Title,
                              Status: {Value: "Pending"},
                              UploadedByID: {Id: gCurrentEmployee.ID, Value: gCurrentEmployee.Title},
                              UploadedAt: Now()
                          }
                      )
                  },
                  If(
                      IsBlank(wBatch.ID),
                      Notify("Failed to create batch. Please try again.", NotificationType.Error),
                      Set(gSelectedBatch, wBatch);
                      Navigate(BatchUploadScreen)
                  )
              )
            Text: ="+ New Batch"
            Width: =140
            X: =Parent.Width - 160
            Y: =12
      - galBatches:
          Control: Gallery@2.15.0
          Variant: BrowseLayout_Vertical_TwoTextOneImageVariant_ver5.0
          Properties:
            Height: =Parent.Height - 76
            Items: =SortByColumns(Filter(BEUI_InvoiceBatch_Requests, UploadedByID.Id = gCurrentEmployee.ID), "UploadedAt", SortOrder.Descending)
            OnSelect: |-
              =Set(gSelectedBatch, ThisItem);
              Navigate(BatchUploadScreen)
            TemplateSize: =70
            Width: =Parent.Width
            Y: =60
          Children:
            - rowBatch:
                Control: GroupContainer@1.5.0
                Variant: AutoLayout
                Properties:
                  DropShadow: =DropShadow.None
                  Height: =70
                  LayoutAlignItems: =LayoutAlignItems.Center
                  LayoutDirection: =LayoutDirection.Horizontal
                  LayoutJustifyContent: =LayoutJustifyContent.SpaceBetween
                  PaddingLeft: =20
                  PaddingRight: =20
                  Width: =Parent.TemplateWidth
                Children:
                  - lblBatchTitle:
                      Control: Label@2.5.1
                      Properties:
                        FontWeight: =FontWeight.Bold
                        Size: =11
                        Text: =ThisItem.Title
                        Width: =450
                  - lblBatchStatus:
                      Control: Label@2.5.1
                      Properties:
                        Color: |-
                          =Switch(
                              ThisItem.Status.Value,
                              "Completed", RGBA(30, 140, 70, 1),
                              "Completed with errors", RGBA(200, 120, 0, 1),
                              "Parsing", RGBA(56, 96, 178, 1),
                              RGBA(120, 120, 120, 1)
                          )
                        Align: =Align.Right
                        FontWeight: =FontWeight.Bold
                        Size: =10
                        Text: =ThisItem.Status.Value
                        Width: =200
